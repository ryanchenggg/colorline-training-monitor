# Transient reports (generated per-run, uploaded as artifacts)
monitor_report.md
daily_digest.md

# Secrets — NEVER commit service account keys
*.json
!training_status.json

# Python
__pycache__/
*.pyc
